import React, { createContext, useContext, useRef } from 'react';


// Default parameters for gestures to be handled
const DEFAULT_MIN_CERTAINTY = 0.6;
const DEFAULT_DURATION = 500;


const GestureHandler = () => {



    // Maps the Gestures 
    const registerGesture = () => {





    };

    // Checks and Validates the parameters of incoming gestures
    const processGestures = () => {




    };




};

export default GestureHandler;